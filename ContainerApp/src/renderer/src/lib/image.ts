/** Constantes de imagen compartidas por las mini apps (drag/drop + previsualización). */

/** Tipos MIME aceptados al subir una imagen (drop zone + file picker). */
export const IMAGE_ACCEPT = ['image/jpeg', 'image/png', 'image/webp']

/** Fondo cuadriculado para previsualizar transparencia (mismo look en todas las apps). */
export const CHECKER =
  'repeating-conic-gradient(hsl(var(--muted)) 0% 25%, transparent 0% 50%) 50% / 22px 22px'
